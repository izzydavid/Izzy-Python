#List Methods 3
basket = ['a', 'x', 'b', 'c', 'd', 'e', 'd']

# basket.sort() # Sort is a built-in-function that sorts the list.
# new_basket = basket[:] # List slicing
# new_basket = basket.copy()  # Returns a copy of a new list.
basket.sort()
basket.reverse()
# print(sorted(basket))  # Sorted produces a new list.
# print(new_basket)
print(basket)
